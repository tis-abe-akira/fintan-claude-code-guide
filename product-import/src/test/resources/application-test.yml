spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

  flyway:
    enabled: false

logging:
  level:
    root: DEBUG
    com.example.productimport: DEBUG
    org.springframework.batch: DEBUG
    org.mybatis: DEBUG
