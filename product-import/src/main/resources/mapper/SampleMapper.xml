<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.productimport.mapper.SampleMapper">

  <resultMap id="sampleEntityResultMap" type="com.example.productimport.entity.SampleEntity">
    <id property="id" column="id"/>
    <result property="name" column="name"/>
    <result property="status" column="status"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
  </resultMap>

  <select id="selectAll" resultMap="sampleEntityResultMap">
    SELECT
      id,
      name,
      status,
      created_at,
      updated_at
    FROM sample_entity
    ORDER BY id
  </select>

  <select id="selectById" resultMap="sampleEntityResultMap">
    SELECT
      id,
      name,
      status,
      created_at,
      updated_at
    FROM sample_entity
    WHERE id = #{id}
  </select>

  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO sample_entity (
      name,
      status,
      created_at,
      updated_at
    ) VALUES (
      #{name},
      #{status},
      #{createdAt},
      #{updatedAt}
    )
  </insert>

  <update id="update">
    UPDATE sample_entity
    SET
      name = #{name},
      status = #{status},
      updated_at = #{updatedAt}
    WHERE id = #{id}
  </update>

</mapper>
